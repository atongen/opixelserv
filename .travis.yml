matrix:
  include:
    - os: linux
      dist: focal
      before_install:
        - sudo apt-get -y install opam libev-dev
    - os: osx
      osx_image: xcode12.2
      before_install:
        - homebrew install opam
install: scripts/deps.sh
script: scripts/compile.sh
